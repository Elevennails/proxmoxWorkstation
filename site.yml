---
- name: Proxmox workstation build
  hosts: localhost
  become: true
  gather_facts: true
  vars_prompt:
    - name: workstation_user
      prompt: "Username to create on the host"
      private: false
  roles:
    - user
    - packages
    - neovim
    - dotfiles
    - desktop

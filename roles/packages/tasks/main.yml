---
- name: Install workstation apt packages
  ansible.builtin.apt:
    name: "{{ workstation_apt_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
    install_recommends: false
